# Claude
.claude/
.worktrees/

# Node
node_modules/

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# Never commit real data to the public repo
*.beancount
# Design mockups at the repo root are scratch working documents about REAL institutions and
# accounts, which is exactly what this repo is meant not to carry. apps/web/mockups/ is tracked
# because its sample data is fictional.
/mockups/
!apps/api/tests/fixtures/*.beancount
!apps/api/tests/fixtures/**/*.beancount
*.xlsx
*.csv
!apps/api/tests/**/*.csv

# Frontend (apps/web)
data.json
.svelte-kit/
package-lock.json

# macOS
.DS_Store
