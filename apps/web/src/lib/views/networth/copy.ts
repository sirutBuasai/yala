// The Net Worth pages' wording, in one file so the two ranges and the planning panel cannot say the
// same thing three ways. Wording shared with other pages lives in `$lib/copy`.
//
// Titles are sentence case with no period; captions are lowercase with no period.

export const TREND = 'Net worth & assets';
export const LIABILITIES = 'Liabilities';
export const ALLOCATION = 'Asset allocations';
export const BUCKET_CHANGE = 'Change by asset type';
export const PROGRESS = 'Financial progress';
export const PROJECTION = 'Projected investments';
export const PLANNING = 'Financial planning';

/** Reads as one phrase with the period it covers appended: "You vs the market, by year". */
export const ATTRIBUTION = 'You vs the market';

export const ALLOCATION_CAPTION = 'dollar amount and shares by asset type';
export const ATTRIBUTION_CAPTION = 'direct savings vs market gains + other income';
export const PROGRESS_CAPTION = 'key metrics for financial independence';
export const PROJECTION_CAPTION = 'invested balance growth and financial independence projection';
export const PLANNING_CAPTION = 'assumptions behind the independence and runway targets';

/** The three levels every snapshot table reports; the period it compares over is appended. */
export const SNAPSHOT_LEVELS = 'changes to net worth, assets, and liabilities';
